---
import { AvButton, AvCard, AvContainer } from "@ansiversa/components";
import AppShell from "../layouts/AppShell.astro";

const title = "Ansiversa App Starter";
const description = "A golden mini-app starter with shared shells, auth guard, and example CRUD.";
---

<AppShell title={title} description={description}>
  <section class="av-section">
    <AvContainer>
      <div class="av-auth-stack-lg">
        <AvCard className="av-auth-card">
          <div class="av-auth-stack-md">
            <div class="av-auth-stack-xs">
              <p class="av-kicker">App Starter</p>
              <h1 class="av-section-title">Build your next mini-app faster</h1>
              <p class="av-text-soft">
                This repo is the official Ansiversa seed. It includes shared layout shells, auth
                guard, notifications badge wiring, and a deletable Example Items module.
              </p>
            </div>

            <div class="av-row-wrap">
              <AvButton href="/items">Open Example Items</AvButton>
              <AvButton href="/admin/items" variant="ghost">Admin Items</AvButton>
            </div>
          </div>
        </AvCard>
      </div>
    </AvContainer>
  </section>
</AppShell>
